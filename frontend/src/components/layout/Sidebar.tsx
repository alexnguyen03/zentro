// Layout Sidebar — thin wrapper that delegates to the full sidebar implementation.
// Kept as a separate file so App.tsx doesn't need to know the inner structure.
export { Sidebar } from '../sidebar/Sidebar';
